# 0.1.0 — 2026-09-03

Première release taguée de `ai-playbook`.

### Fonctionnalités

- Ajoute `rules/context-efficiency.md` (lecture ciblée, exploration minimale, tests ciblés avant
  complets, sous-agents à bon escient, découpage de session) et l'applique à `start-ticket`
  (regroupement en phases Analyse/Implémentation/Review, rappel `/clear`, gabarit
  `plan-template.md`).
- Adopte le modèle Git Flow (skill, agent `git-flow-reviewer`, rule `git-flow.md`) pour ce repo.
- Ajoute un hook Stop qui vérifie que le tableau "Inventaire" du `README.md` reste à jour.
- Scinde un ancien skill de release spécifique à un projet en deux skills génériques : `release`
  et `generate-release-note`.

### Documentation

- `rules/css-units.md` + renvoi depuis `general-coding.md`.
- `develop` devient la branche par défaut du repo sur GitHub.
- `rules/git-flow.md` : piège du mot-clé de fermeture (`Closes #N`) vs branche par défaut avec un
  tracker GitHub Issues, et limite constatée (testée) du workflow natif Projects v2
  *Pull request linked to issue*, qui ne se déclenche pas sur une simple mention (`Refs #N`).

### Autres

- Exploration d'une synchronisation du kanban Projects v2 par script/GitHub Action, abandonnée au
  profit des automatisations natives du projet (plus simples, sans script à maintenir).
