.DS_Store
*.log
.idea/
.vscode/

# Ban words locaux (peut contenir de vrais noms de client/projet) — jamais commité, voir
# .claude/hooks/block-banwords.py et rules/hooks.md.
.claude/hooks/banwords.local.txt

# Fichier de travail éphémère du workflow release-notes.yml (écrit par le runner GitHub Actions
# dans le workspace, jamais commité — au cas où generate.sh est rejoué en local).
/release-notes.md
